# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================
# Supabase project URL (found in Project Settings > API)
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous/public key (found in Project Settings > API)
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key_here

# ============================================================================
# STRIPE CONFIGURATION (Frontend)
# ============================================================================
# Stripe publishable key (starts with pk_test_ or pk_live_)
# Found in Stripe Dashboard > Developers > API keys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# ============================================================================
# SUPABASE SECRETS (Edge Functions - Set via Supabase CLI)
# ============================================================================
# These secrets should be set using the Supabase CLI, not in .env files:
# supabase secrets set KEY_NAME=value

# --- OpenAI ---
# OpenAI API key for script and prompt generation
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# --- Cartesia AI ---
# Cartesia API key for text-to-speech voice generation
# Get from: https://cartesia.ai/
# CARTESIA_API_KEY=...

# --- Fal.ai ---
# Fal.ai API key for image and video generation
# Get from: https://fal.ai/dashboard
# FAL_KEY=...
# (Alternative names: FAL_API_KEY, FALAI_API_KEY, FAL_AI_API_KEY)

# --- Stripe (Backend) ---
# Stripe secret key (starts with sk_test_ or sk_live_)
# Found in Stripe Dashboard > Developers > API keys
# STRIPE_SECRET_KEY=sk_test_...

# Stripe webhook secret (starts with whsec_)
# Found in Stripe Dashboard > Developers > Webhooks
# After creating webhook endpoint, copy the signing secret
# STRIPE_WEBHOOK_SECRET=whsec_...

# ============================================================================
# DEPLOYMENT CONFIGURATION
# ============================================================================

# Base URL for the application (default: /)
# Set this if deploying to a subdirectory
# VITE_BASE_URL=/

# ============================================================================
# STRIPE SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Create a Stripe account at https://stripe.com
#
# 2. In Stripe Dashboard, create products and prices:
#    - Go to Products > Add product
#    - Create 3 products: Starter (69€), Pro (129€), Business (169€)
#    - Note the Price IDs (price_xxxxx)
#
# 3. Update subscription_plans table with Stripe Price IDs:
#    UPDATE subscription_plans
#    SET stripe_price_id = 'price_xxxxx', stripe_product_id = 'prod_xxxxx'
#    WHERE name = 'starter';
#
# 4. Set up webhook endpoint:
#    - Go to Stripe Dashboard > Developers > Webhooks
#    - Add endpoint: https://YOUR_PROJECT.supabase.co/functions/v1/stripe-webhook
#    - Select events:
#      * checkout.session.completed
#      * customer.subscription.created
#      * customer.subscription.updated
#      * customer.subscription.deleted
#      * invoice.payment_succeeded
#      * invoice.payment_failed
#    - Copy the webhook signing secret (whsec_...)
#
# 5. Configure Supabase secrets:
#    supabase secrets set STRIPE_SECRET_KEY=sk_test_...
#    supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_...
#    supabase secrets set OPENAI_API_KEY=sk-...
#    supabase secrets set CARTESIA_API_KEY=...
#    supabase secrets set FAL_KEY=...
#
# 6. Deploy Edge Functions:
#    supabase functions deploy stripe-checkout
#    supabase functions deploy stripe-webhook
#    supabase functions deploy stripe-portal
#    supabase functions deploy check-subscription
#
# 7. Apply database migrations:
#    supabase db push
#
# 8. Test the integration:
#    - Use Stripe test cards: 4242 4242 4242 4242
#    - Trigger test webhooks from Stripe CLI or Dashboard
#
# ============================================================================
# ADDITIONAL NOTES
# ============================================================================
#
# - Never commit .env or .env.local files to version control
# - Use .env.local for local development (automatically ignored by git)
# - Production secrets should be set in Supabase Dashboard or via CLI
# - Stripe test mode vs live mode:
#   * Test keys start with pk_test_ or sk_test_
#   * Live keys start with pk_live_ or sk_live_
#   * Always use test mode during development
#
# - Frontend environment variables MUST start with VITE_ to be accessible
# - Backend secrets (Edge Functions) are set via Supabase CLI
#
# ============================================================================
